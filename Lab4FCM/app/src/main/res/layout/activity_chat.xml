<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_chat"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="co.edu.udea.compumovil.gr01.lab4fcm.ChatActivity">

    <ListView
       android:layout_width="match_parent"
       android:layout_height="432dp"
        android:id="@+id/list_rooms"
        ></ListView>

    <Button
        android:layout_width="115dp"
        android:layout_height="wrap_content"
        android:text="add chat"
        android:layout_gravity="bottom|right"
        android:id="@+id/btn_add_chat"/>

    <EditText
        android:layout_width="210dp"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|left"
        android:hint="Crea una sala de chat"
        android:layout_marginLeft="10dp"
        android:layout_marginStart="10dp"
        android:id="@+id/room_name_edittext"
        android:layout_toLeftOf="@+id/btn_add_chat"
        android:layout_toStartOf="@+id/btn_add_chat" />

</FrameLayout>
